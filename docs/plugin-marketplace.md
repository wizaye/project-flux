# Flux plugin marketplace

Marketplace is separate repository. Plugin source and release artifacts remain in publisher repositories.

## Registry repository

```text
flux-plugin-registry/
├── submissions/<plugin-id>.json
├── registry.json
├── registry.json.sig
└── public-key.txt
```

Each submission names publisher repository and release asset. Registry build job:

1. Downloads release `.flux-plugin`.
2. Computes SHA-256.
3. Opens archive and validates `flux.plugin.json`.
4. Verifies submitted manifest matches packaged manifest.
5. Fetches publisher `README.md` and stores a bounded snapshot in generated index.
6. Emits deterministic `registry.json`.
7. Signs exact generated bytes with Ed25519.

Generated entry:

```json
{
  "manifest": {
    "schemaVersion": 1,
    "id": "acme.calendar",
    "name": "Calendar",
    "publisher": "Acme",
    "version": "1.2.0",
    "apiVersion": "1",
    "entry": "dist/main.js",
    "requiredPermissions": ["vault.read"],
    "optionalPermissions": []
  },
  "publisher": "Acme",
  "repository": "https://github.com/acme/flux-calendar",
  "downloadUrl": "https://github.com/acme/flux-calendar/releases/download/v1.2.0/acme.calendar-1.2.0.flux-plugin",
  "sha256": "<64 lowercase hex characters>",
  "readme": "# Calendar\n...",
  "changelog": "Added week view.",
  "publishedAt": "2026-07-23T00:00:00Z"
}
```

Top-level index uses:

```json
{"schemaVersion":1,"updatedAt":"2026-07-23T00:00:00Z","plugins":[]}
```

## Signing

Generate key once, offline:

```sh
openssl genpkey -algorithm ED25519 -out registry-private.pem
openssl pkey -in registry-private.pem -pubout -outform DER |
  tail -c 32 |
  base64 > public-key.txt
```

Sign exact index bytes:

```sh
openssl pkeyutl -sign -rawin \
  -inkey registry-private.pem \
  -in registry.json |
  base64 > registry.json.sig
```

Private key must not enter registry repository. Use protected CI environment or offline release signing. Public key ships through deployment configuration:

```text
FLUX_PLUGIN_REGISTRY_URL=https://plugins.flux.example/registry.json
FLUX_PLUGIN_REGISTRY_SIGNATURE_URL=https://plugins.flux.example/registry.json.sig
FLUX_PLUGIN_REGISTRY_PUBLIC_KEY=<public-key.txt contents>
```

Flux verifies signature before showing marketplace, then verifies downloaded package SHA-256 and packaged manifest before staging installation. Marketplace remains disabled when these variables are absent.

Landing page may fetch `registry.json` directly for public listings. Install security remains server-side inside Flux.

## Publisher workflow

Publisher works entirely outside Flux monorepo:

```sh
bunx create-flux-plugin my-plugin
cd my-plugin
bun install
bun run validate
bun run pack
```

Publisher creates release, then opens registry PR containing metadata only. Registry CI copies README snapshot and release metadata into signed index.
